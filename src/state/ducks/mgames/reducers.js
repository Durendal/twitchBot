import * as types from './types';

const initialMoneyGameState = {
  channels: [],
};

const mgameReducer = (state = initialMoneyGameState, action) => {

  const { type, payload } = action;

  switch (type) {
    
  };
};

export default mgameReducer;
