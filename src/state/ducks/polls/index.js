import reducer from './reducers';

import * as pollOperations from './operations';
import * as pollSelectors from './selectors';
import * as pollTypes from './types';

export {
  pollOperations,
  pollSelectors,
  pollTypes
};

export default reducer;
