import reducer from './reducers';

import * as botOperations from './operations';
import * as botSelectors from './selectors';
import * as botTypes from './types';

export {
  botOperations,
  botSelectors,
  botTypes
};

export default reducer;
