import reducer from './reducers';

import * as scraperOperations from './operations';
import * as scraperSelectors from './selectors';
import * as scraperTypes from './types';

export {
  scraperOperations,
  scraperSelectors,
  scraperTypes
};

export default reducer;
