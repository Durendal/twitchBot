import reducer from './reducers';

import * as commandsOperations from './operations';
import * as commandsSelectors from './selectors';
import * as commandsTypes from './types';

export {
  commandsOperations,
  commandsSelectors,
  commandsTypes
};

export default reducer;
